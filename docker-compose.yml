services:
  postgres:
    image: registry.cn-hangzhou.aliyuncs.com/lacogito/postgres:17.5-alpine3.21
    container_name: alithia-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-alithia}
      POSTGRES_USER: ${POSTGRES_USER:-alithia}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-alithia}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-alithia} -d ${POSTGRES_DB:-alithia}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - internal

  alithia:
    image: registry.cn-hangzhou.aliyuncs.com/lacogito/alithia:latest
    container_name: alithia-app
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ALITHIA_CONFIG_PATH: /app/alithia_config.json
      ALITHIA_POSTGRES_DSN: "postgresql://${POSTGRES_USER:-alithia}:${POSTGRES_PASSWORD:-alithia}@postgres:5432/${POSTGRES_DB:-alithia}"
      LITELLM_LOCAL_MODEL_COST_MAP: "true"
    volumes:
      - ./alithia_config.json:/app/alithia_config.json:ro
      - app_data:/app/data
    networks:
      - internal
      - default
    restart: unless-stopped

  nginx:
    image: registry.cn-hangzhou.aliyuncs.com/lacogito/nginx:1.27-alpine3.21
    container_name: alithia-nginx
    depends_on:
      - alithia
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "${APP_PORT:-8080}:80"
    networks:
      - internal
      - default
    restart: unless-stopped

volumes:
  postgres_data:
  app_data:

networks:
  internal:
    driver: bridge
    internal: true
